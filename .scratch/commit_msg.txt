refactor: siliconflow 彻底降级为 multimodal 的平台示例,统一到通用环境变量

引擎类型收敛为两个(rapidocr / multimodal)。siliconflow 不再是独立类型,
只是 multimodal 的注册别名:写 name: siliconflow 与写 name: multimodal 完全
等价,返回结果里的 engine 字段一律是 multimodal。平台由条目里的 base_url +
model 决定,不是引擎名。

代码:
- 删除 SiliconFlowEngine 子类,删除 @register("siliconflow") 工厂与惰性导入项
- 删除 config 里的 vendor 默认值:siliconflow 不再有内置 base_url / model。
  base_url 解析不出即 EngineNotAvailable,而不是静默指向某个平台——否则
  A 平台的 key 会被发给 B 平台,最后只表现为一个说不清原因的 HTTP 401
- remote_engines() 内置名单收敛为 ("multimodal",)
- 唯一保留的默认是模型名:留空时用裸 ID PaddleOCR-VL-1.5,只适用于认裸 ID
  的平台

环境变量:
- 删除 SILICONFLOW_API_KEY / JYKJ_OCR_SILICONFLOW_MODEL,改用通用
  OPENAI_API_KEY / OPENAI_BASE_URL / JYKJ_OCR_MULTIMODAL_*
- .env.example 同步,并注明 MULTIMODAL 之外没有第二个厂商变量

文档:
- README / manual / index / architecture(mmd + html + archify)全部改为
  两个引擎类型;siliconflow 只作为平台示例与 base_url 出现
- 新增"平台不是引擎"说明与配置优先级里的 fail-fast 理由
- 212 个用例基线

脚本:
- 删除 scripts/test_interfaces.py(功能已并入 demo.py)
- probe_siliconflow.py 改读通用变量,并去掉打印 key 尾 6 位的语句
- real_model_e2e.py 补 _load_dotenv():之前 key 只写进 .env 时泄露检测拿不到
  片段,那条"不泄露 key"的用例会因检查未生效而失败,而不是真的泄露
