# jykj_ocr 环境变量参考
#
# .env 是可选的：只在进程启动时读一次（cli.py / server.py 各读一次），
# 没有这个文件也完全正常。推荐直接用 export 或 Docker -e 注入，本仓库不保留 .env。
# 想用外部密钥文件，可以：cp .env.example .env  或  docker compose --env-file <路径> up
# （--env-file 的文件可放在项目外，无需入库）。
#
# 远程 OCR 引擎统一走 OpenAI 兼容协议：单平台只需设置下面这一对变量，
# 即可指向任意平台（硅基流动 / 阿里云百炼 / 智谱 / 本地 vLLM 等）。
# 平台由条目里的 base_url + model 决定，不是引擎名——没有内置默认 URL，
# 解析不出 base_url 就报 EngineNotAvailable，不会静默指向某个平台。

# === 首选：通用 OpenAI 兼容端点（适配任意平台） ===
OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.moark.com/v1
# 换平台只改上面两行（代码与 config.yaml 都不用动）：
#   模力方舟：  https://api.moark.com/v1        （裸模型 ID，如 PaddleOCR-VL-1.5）
#   硅基流动：  https://api.siliconflow.cn/v1   （带厂商前缀，如 PaddlePaddle/PaddleOCR-VL-1.5）
#   阿里云百炼：https://dashscope.aliyuncs.com/compatible-mode/v1
#   火山方舟：  https://ark.cn-beijing.volces.com/api/v3
#   智谱：      https://open.bigmodel.cn/api/paas/v4
#   本地 vLLM： http://localhost:8000/v1

# === 多 multimodal 实例：每个字段都有按序号的变量 ===
# config.yaml 里可以写任意多条 name: multimodal（同厂商不同模型 / 不同厂商 /
# 同厂商不同账号）。序号是「同一类型里的第 N 条」，按 yaml 里的书写顺序编号，
# 数量不限；rapidocr 不占 multimodal 的号。
#
#   第 1 条 → OPENAI_BASE_URL_1
#            JYKJ_OCR_MULTIMODAL_1_API_KEY   （或简写 MULTIMODAL_1_API_KEY）
#            JYKJ_OCR_MULTIMODAL_1_MODEL
#   第 2 条 → OPENAI_BASE_URL_2 / JYKJ_OCR_MULTIMODAL_2_API_KEY / _2_MODEL
#   第 N 条 → OPENAI_BASE_URL_<N> / JYKJ_OCR_MULTIMODAL_<N>_{API_KEY,MODEL}
#
# 每条各自解析（先命中先用）：
#   base_url  条目里的值 → OPENAI_BASE_URL_<N> → OPENAI_BASE_URL
#             → JYKJ_OCR_MULTIMODAL_BASE_URL
#   api_key   条目里的值 → JYKJ_OCR_MULTIMODAL_<N>_API_KEY → MULTIMODAL_<N>_API_KEY
#             → JYKJ_OCR_MULTIMODAL_API_KEY → MULTIMODAL_API_KEY → OPENAI_API_KEY
#   model     条目里的值 → JYKJ_OCR_MULTIMODAL_<N>_MODEL → JYKJ_OCR_MULTIMODAL_MODEL
#             → 退化默认 PaddleOCR-VL-1.5
#
# 不带序号的变量被同类型所有条目共享——只有单平台时才够用。base_url / model /
# api_key 三样都可以按序号写，所以整个条目（含端点与模型）都可以只靠环境变量
# 描述，yaml 里一个字符都不用写。
# 典型症状：某平台返回 HTTP 401 "Token is invalid" —— 你把 A 平台的 key 发给了
# B 平台，多半是两条共用了一把不带序号的 key。
#
# 想让序号不随条目增删漂移，可在 yaml 里显式钉住：
#   - name: multimodal
#     instance: 1        # 固定读 *_1_*；未钉住的条目自动跳过这个号
#
# 单平台多模型最省事：多条共用 OPENAI_API_KEY，只在各条目写不同的 model。

# === 可选：引擎专属变量 ===
# 所有 multimodal 条目共用同一把 key 时用（单平台多模型场景）：
# JYKJ_OCR_MULTIMODAL_API_KEY=sk-your-key-here
# 只保留 MULTIMODAL 这一对：siliconflow 已是 multimodal 的别名，
# JYKJ_OCR_SILICONFLOW_* / SILICONFLOW_API_KEY 不再被读取。
# 覆盖 multimodal 默认模型（按类型共享;多平台请写进各条目或用 *_<N>_MODEL）：
# JYKJ_OCR_MULTIMODAL_MODEL=PaddleOCR-VL-1.5

# === 服务配置 ===
# JYKJ_OCR_CONFIG=config/config.yaml
# JYKJ_OCR_PORT=8000
# 把新厂商划入远程侧（vl / bestof 预设才会选它），逗号分隔：
# JYKJ_OCR_REMOTE_ENGINES=multimodal
