# jykj_ocr 环境变量示例
# 复制为 .env 并填入真实值：  cp .env.example .env
# .env 已在 .gitignore 中，不会被提交。
#
# 远程 OCR 引擎统一走 OpenAI 兼容协议：只需设置下面这一对变量，
# 即可指向任意平台（硅基流动 / 阿里云百炼 / 智谱 / 本地 vLLM 等）。
# siliconflow 引擎在不设这对变量时仍会用其内置默认 URL，无需额外配置。

# === 首选：通用 OpenAI 兼容端点（适配任意平台） ===
OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.moark.com/v1
# 换平台只改上面两行（代码与 config.yaml 都不用动）：
#   模力方舟：  https://api.moark.com/v1        （裸模型 ID，如 PaddleOCR-VL-1.5）
#   硅基流动：  https://api.siliconflow.cn/v1   （带厂商前缀，如 PaddlePaddle/PaddleOCR-VL-1.5）
#   阿里云百炼：https://dashscope.aliyuncs.com/compatible-mode/v1
#   火山方舟：  https://ark.cn-beijing.volces.com/api/v3
#   智谱：      https://open.bigmodel.cn/api/paas/v4
#   本地 vLLM： http://localhost:8000/v1

# === 可选：引擎专属变量 ===
# 仅当某一引擎需要与 OPENAI_* 不同的 key 时才设：
# SILICONFLOW_API_KEY=sk-your-siliconflow-key
# 覆盖 siliconflow 默认模型（换 Qwen3-VL 等视觉模型）：
# JYKJ_OCR_SILICONFLOW_MODEL=PaddlePaddle/PaddleOCR-VL-1.5
# 覆盖 multimodal 默认模型（单平台多模型场景，key 仍走 OPENAI_API_KEY）：
# JYKJ_OCR_MULTIMODAL_MODEL=PaddleOCR-VL-1.5

# === 多 multimodal 实例的重要限制 ===
# config.yaml 里可以写任意多条 name: multimodal（同厂商不同模型 / 不同厂商 /
# 同厂商不同账号），但环境变量按「类型」命名，不按「实例」命名：
#
#   JYKJ_OCR_MULTIMODAL_API_KEY   ← 所有 multimodal 条目共享这一个 key
#   JYKJ_OCR_MULTIMODAL_MODEL     ← 所有 multimodal 条目共享这一个 model 回退值
#   OPENAI_BASE_URL               ← 唯一生效的 base URL 环境变量，同样被所有条目共享
#
# 代码里没有 JYKJ_OCR_MULTIMODAL_BASE_URL —— 多平台的 base_url 只能写在
# config.yaml 的条目里。所以一个 key 无法同时服务两个不同平台或同一平台的
# 两个账号。典型症状：某平台返回 HTTP 401 "Token is invalid" —— 你把 A 平台的
# key 塞进了 B 平台的条目。
#
# 解决方式二选一：
#   a) 在 config.yaml 条目里显式写 base_url 和 model，把不属于当前 key 的条目
#      设为 enabled: false（推荐；yaml 可入库，key 不落盘）
#   b) 在 config.yaml 条目里直接写 api_key（仅当该 yaml 已加入 .gitignore 时安全）
#
# 单平台多模型是安全的：多条 multimodal 共用同一个 JYKJ_OCR_MULTIMODAL_API_KEY
# 或 OPENAI_API_KEY，只在各条目里写不同的 model 即可。

# === 服务配置 ===
# JYKJ_OCR_CONFIG=config/config.yaml
# JYKJ_OCR_PORT=8000
# 把新厂商划入远程侧（vl / bestof 预设才会选它），逗号分隔：
# JYKJ_OCR_REMOTE_ENGINES=multimodal,siliconflow
